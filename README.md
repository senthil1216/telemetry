##Telemetry
Developed using Flask Python framework, this framework captures all user events from client side and sends the data to REST API exposed from server.

The REST API then creates log files of the captured data which can be eventually used by hadoop system to process it.

